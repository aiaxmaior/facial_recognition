{
    "device_id": "cam-001",
    "broker_url": "https://acetaxi-bridge.qryde.net/iot-broker/api",
    "api_key": null,
    
    "sync": {
        "interval_minutes": 15,
        "model": "ArcFace"
    },
    
    "event_validation": {
        "confirmation_frames": 5,
        "consistency_threshold": 0.8,
        "cooldown_seconds": 30,
        "distance_threshold": 0.35
    },
    
    "image_compression": {
        "enabled": true,
        "quality": 65,
        "max_size_kb": 50,
        "max_dimension": 320
    },
    
    "event_queue": {
        "max_size": 1000,
        "batch_size": 10,
        "batch_timeout_ms": 5000
    },
    
    "connection": {
        "timeout_seconds": 30,
        "max_retries": 3,
        "retry_backoff": 0.5,
        "verify_ssl": true
    },
    
    "dev_mode": false,
    
    "_comments": {
        "device_id": "Unique identifier for this edge device",
        "broker_url": "IoT broker endpoint URL",
        "sync.interval_minutes": "How often to pull enrollment updates",
        "event_validation.confirmation_frames": "Consecutive frames needed to confirm identity",
        "event_validation.consistency_threshold": "Ratio of frames that must have same user_id",
        "event_validation.cooldown_seconds": "Time before same user can trigger new event",
        "dev_mode": "If true, display_name is stored locally for debugging"
    }
}
